<p id="notice"><%= notice %></p>

<div class="row">
  <div class="container">
    <h1>
      <%= @user.first_name.concat(' ').concat(@user.last_name) %>
      <%= link_to 'Y', edit_user_path(@user) %>
    </h1>
  </div>
</div>

<div class="row">
  <div class="container">
    <h2>Nail Polish that I Own</h2>
    <br />
    <% @user.bottles.each do |bottle| %>
      <div class="bottle">
        <img src="<%= asset_path('bottles/' + bottle.image_path) %>" />
      </div>
    <% end %>
    <br /><br />
  </div>
</div>
